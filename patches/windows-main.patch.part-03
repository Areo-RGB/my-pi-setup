       "resolved": "https://registry.npmjs.org/cross-spawn/-/cross-spawn-7.0.6.tgz",
       "integrity": "sha512-uV2QOWP2nWzsy2aMp8aRibhi9dlzF5Hgh5SHaB9OiTGEyDTiJJyx0uy51QXdyWbtAHNua4XJzUKca3OzKUd3vA==",
       "license": "MIT",
-      "peer": true,
       "dependencies": {
         "path-key": "^3.1.0",
         "shebang-command": "^2.0.0",
@@ -1475,8 +1475,7 @@
       "version": "2.0.0",
       "resolved": "https://registry.npmjs.org/isexe/-/isexe-2.0.0.tgz",
       "integrity": "sha512-RHxMLp9lnKHGHRng9QFhRCMbYAcVpn69smSGcq3f36xjgVVWThj4qqLbTLlq7Ssj8B+fIQ1EuCEGI2lKsyQeIw==",
-      "license": "ISC",
-      "peer": true
+      "license": "ISC"
     },
     "node_modules/jose": {
       "version": "6.2.4",
@@ -1716,7 +1715,6 @@
       "resolved": "https://registry.npmjs.org/path-key/-/path-key-3.1.1.tgz",
       "integrity": "sha512-ojmeN0qd+y0jszEtoY48r0Peq5dwMEkIlCOu6Q5f41lfkswXuKtYrhgoTpLnyIcHm24Uhqx+5Tqm2InSwLhE6Q==",
       "license": "MIT",
-      "peer": true,
       "engines": {
         "node": ">=8"
       }
@@ -1912,7 +1910,6 @@
       "resolved": "https://registry.npmjs.org/shebang-command/-/shebang-command-2.0.0.tgz",
       "integrity": "sha512-kHxr2zZpYtdmrN1qDjrrX/Z1rR1kG8Dx+gkpK1G4eXmvXswmcE1hTWBWYUzlraYw1/yZp6YuDY77YtvbN0dmDA==",
       "license": "MIT",
-      "peer": true,
       "dependencies": {
         "shebang-regex": "^3.0.0"
       },
@@ -1925,7 +1922,6 @@
       "resolved": "https://registry.npmjs.org/shebang-regex/-/shebang-regex-3.0.0.tgz",
       "integrity": "sha512-7++dFhtcx3353uBaq8DDR4NuxBetBzC7ZQOhmTQInHEd6bSrXdiEyzCvG07Z44UYdLShWUyXt5M/yhz8ekcb1A==",
       "license": "MIT",
-      "peer": true,
       "engines": {
         "node": ">=8"
       }
@@ -2159,7 +2155,6 @@
       "resolved": "https://registry.npmjs.org/which/-/which-2.0.2.tgz",
       "integrity": "sha512-BLI3Tl1TW3Pvl70l3yq3Y64i+awpwXqsGBYWkkqMtnbXgrMD+yj7rhW0kuEDxzJaYXGjEW5ogapKNMEKNMjibA==",
       "license": "ISC",
-      "peer": true,
       "dependencies": {
         "isexe": "^2.0.0"
       },
diff --git a/extensions/subagents/package.json b/extensions/subagents/package.json
index 323f217..838c2cf 100644
--- a/extensions/subagents/package.json
+++ b/extensions/subagents/package.json
@@ -10,7 +10,8 @@
   },
   "dependencies": {
     "@anthropic-ai/claude-agent-sdk": "^0.3.216",
-    "effect": "^4.0.0-beta.99"
+    "effect": "^4.0.0-beta.99",
+    "cross-spawn": "^7.0.6"
   },
   "devDependencies": {
     "@effect/tsgo": "^0.24.2",
diff --git a/extensions/subagents/src/backends/claude.ts b/extensions/subagents/src/backends/claude.ts
index 5eb00e2..03804f7 100644
--- a/extensions/subagents/src/backends/claude.ts
+++ b/extensions/subagents/src/backends/claude.ts
@@ -52,18 +52,19 @@ function executable(file: string) {
 }
 
 /**
- * Resolve the Claude Code CLI once from PATH. The SDK can also run without
- * this (it bundles a CLI), but pointing it at the user's installed binary
- * keeps versions, settings, and login state consistent with their terminal.
+ * Resolve an optional native Claude Code override from PATH. The Agent SDK
+ * already bundles a per-platform native binary, so Windows npm shims are
+ * deliberately ignored: `.cmd` files cannot be passed to CreateProcess as
+ * normal executables.
  */
 function resolveClaudeBinary() {
   if (cachedClaudeBinary !== undefined) return cachedClaudeBinary ?? undefined;
-  const names =
-    process.platform === "win32"
-      ? ["claude.exe", "claude.cmd", "claude"]
-      : ["claude"];
-  for (const directory of (process.env.PATH ?? "").split(path.delimiter)) {
-    if (!directory) continue;
+  const names = process.platform === "win32" ? ["claude.exe"] : ["claude"];
+  const directories = (process.env.PATH ?? "")
+    .split(path.delimiter)
+    .map((directory) => directory.replace(/^"|"$/g, ""))
+    .filter(Boolean);
+  for (const directory of directories) {
     for (const name of names) {
       const candidate = path.join(directory, name);
       if (executable(candidate)) {
@@ -696,6 +697,9 @@ const makeClaudeSession = (
 export const claudeBackend: SubagentBackend = {
   name: "claude",
   capabilities: { steering: true, modelSelection: true, reasoningEffort: true },
-  available: Effect.sync(() => resolveClaudeBinary() !== undefined),
+  // The SDK package provides the platform binary. A PATH CLI is an optional
+  // override, not an availability requirement. Spawn reports a precise error
+  // if optional dependencies were intentionally omitted.
+  available: Effect.succeed(true),
   spawn: makeClaudeSession,
 };
diff --git a/extensions/subagents/src/backends/codex.ts b/extensions/subagents/src/backends/codex.ts
index e7fb033..5b71f80 100644
--- a/extensions/subagents/src/backends/codex.ts
+++ b/extensions/subagents/src/backends/codex.ts
@@ -10,6 +10,7 @@
  */
 
 import { spawn, type ChildProcessWithoutNullStreams } from "node:child_process";
+import { createRequire } from "node:module";
 import * as fs from "node:fs";
 import * as path from "node:path";
 import type { Cause, Scope } from "effect";
@@ -33,6 +34,12 @@ const PREVIEW_MAX_LENGTH = 1_024;
 /** A protocol line larger than this without a newline means a broken peer. */
 const STDOUT_BUFFER_MAX_BYTES = 4 * 1024 * 1024;
 
+// Node cannot directly spawn npm-generated .cmd/.bat shims on Windows.
+// cross-spawn resolves PATHEXT and safely routes those shims through cmd.exe
+// while keeping argv-based spawning for native executables on every platform.
+const require = createRequire(import.meta.url);
+const crossSpawn = require("cross-spawn") as typeof spawn;
+
 type JsonRecord = Record<string, unknown>;
 
 interface PendingRequest {
@@ -63,10 +70,15 @@ function executable(file: string) {
 function resolveCodexBinary() {
   if (cachedCodexBinary !== undefined) return cachedCodexBinary ?? undefined;
   const names =
-    process.platform === "win32" ? ["codex.exe", "codex.cmd"] : ["codex"];
-  for (const directory of (process.env.PATH ?? "").split(path.delimiter)) {
-    if (!directory) continue;
-    for (const name of names) {
+    process.platform === "win32" ? ["codex.cmd", "codex.exe"] : ["codex"];
+  const directories = (process.env.PATH ?? "")
+    .split(path.delimiter)
+    .map((directory) => directory.replace(/^"|"$/g, ""))
+    .filter(Boolean);
+  // On Windows prefer an npm shim anywhere on PATH over a WindowsApps alias.
+  // The latter can exist but still fail CreateProcess with access/path errors.
+  for (const name of names) {
+    for (const directory of directories) {
       const candidate = path.join(directory, name);
       if (executable(candidate)) {
         cachedCodexBinary = candidate;
@@ -319,7 +331,7 @@ const makeCodexSession = (
 
     const child = yield* Effect.try({
       try: () =>
-        spawn(binary, ["app-server", "--stdio"], {
+        crossSpawn(binary, ["app-server", "--stdio"], {
           cwd: task.cwd,
           env: process.env,
           stdio: ["pipe", "pipe", "pipe"],
diff --git a/package-lock.json b/package-lock.json
index 24df96b..d4886a3 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -16,7 +16,8 @@
         "@types/node": "^26.1.1",
         "prettier": "^3.9.5",
         "typescript": "^7.0.2"
-      }
+      },
+      "hasInstallScript": true
     },
     "node_modules/@anthropic-ai/sdk": {
       "version": "0.91.1",
diff --git a/package.json b/package.json
index 0b44a3b..830d530 100644
--- a/package.json
+++ b/package.json
@@ -17,6 +17,7 @@
     "check": "tsc --noEmit",
     "format": "prettier --write \"extensions/**/*.{ts,cjs}\" \"*.json\"",
     "format:check": "prettier --check \"extensions/**/*.{ts,cjs}\" \"*.json\"",
-    "test": "node --test --experimental-strip-types extensions/*/*.test.ts && npm --prefix extensions/file-search test"
+    "test": "node --test --experimental-strip-types extensions/*/*.test.ts && npm --prefix extensions/file-search test",
+    "postinstall": "node scripts/install-extension-deps.mjs"
   }
 }
diff --git a/scripts/install-extension-deps.mjs b/scripts/install-extension-deps.mjs
new file mode 100644
index 0000000..2bfd05d
--- /dev/null
+++ b/scripts/install-extension-deps.mjs
@@ -0,0 +1,60 @@
+import { spawnSync } from "node:child_process";
+import * as fs from "node:fs";
+import * as path from "node:path";
+import { fileURLToPath } from "node:url";
+
+const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
+const extensionsDir = path.join(root, "extensions");
+
+const packageDirs = fs
+  .readdirSync(extensionsDir, { withFileTypes: true })
+  .filter((entry) => entry.isDirectory())
+  .map((entry) => path.join(extensionsDir, entry.name))
+  .filter((directory) => fs.existsSync(path.join(directory, "package.json")))
+  .filter((directory) => {
+    const pkg = JSON.parse(
+      fs.readFileSync(path.join(directory, "package.json"), "utf8"),
+    );
+    return (
+      Object.keys(pkg.dependencies ?? {}).length > 0 ||
+      Object.keys(pkg.optionalDependencies ?? {}).length > 0
