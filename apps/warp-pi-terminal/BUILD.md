# Warp Pi Terminal build

The complete source is stored in `source.zip.b64` and expanded by the Windows build workflow. The resulting artifact contains `WarpPiTerminal.exe`, the source archive, and a SHA-256 checksum.
